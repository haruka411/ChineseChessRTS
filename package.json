{
  "name": "chinese-chess-rts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "package:itch": "npm run build && powershell -NoProfile -ExecutionPolicy Bypass -Command \"if (Test-Path 'chinese-chess-rts-itch.zip') { Remove-Item -LiteralPath 'chinese-chess-rts-itch.zip' -Force }; Compress-Archive -Path 'dist/*' -DestinationPath 'chinese-chess-rts-itch.zip' -Force\""
  },
  "dependencies": {
    "lucide-react": "^1.17.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
